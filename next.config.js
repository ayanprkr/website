/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['i.scdn.co', 'images.unsplash.com', 'cdn.discordapp.com']
  }
}

module.exports = nextConfig
